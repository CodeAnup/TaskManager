<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Task Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center px-4 py-10">

  <!-- Back Link -->
  <div class="w-full max-w-4xl mb-6 text-left">
    <a href="/" class="inline-block text-indigo-400 hover:text-indigo-300 transition font-medium text-sm">
      ← Back to Home
    </a>
  </div>
  <div class="w-full max-w-xl bg-zinc-800 p-8 rounded-2xl shadow-2xl">
    <form action="/edit" method="post" class="space-y-6">
      <!-- Task Title -->
      <input type="text" name="previous" placeholder="Previous Name" value="<%= filename %>" disabled="true"
        class="w-full px-4 py-3 rounded-xl bg-zinc-700 text-zinc-500 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-zinc-500" required />

        <!--New Task Title -->
      <input type="text" name="new" placeholder="New Name"
        class="w-full px-4 py-3 rounded-xl bg-zinc-700 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-zinc-500" required />

      <!-- Submit Button -->
      <button
        class="w-full bg-yellow-600 hover:bg-yellow-700 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-300">
        ✏️ Update
      </button>
    </form>
  </div>


</body>

</html>
